//import groovy.json.JsonSlurper
//
//def keyFile = null
//if(new File("keys.json").exists()) {
//    keyFile = new File("keys.json")
//}
//else {
//    keyFile = new File("../keys.json");
//}
//def keysJSON = new JsonSlurper().parseText(keyFile.text)

apply plugin: 'com.android.application'

android {
    compileSdkVersion 26
    buildToolsVersion '26.0.2'

    defaultConfig {
        applicationId "com.github.premek.kyosc"
        minSdkVersion 16
        targetSdkVersion 26
        versionCode 7
        versionName '1.0.0'
    }

//    signingConfigs {
//        release {
//            storeFile file(keysJSON.keyStorePath)
//            storePassword keysJSON.keyStorePass
//            keyAlias keysJSON.keyAlias
//            keyPassword keysJSON.keyPassword
//        }
//    }

    buildTypes {
        release {
//            runProguard false
//            debuggable false
//            jniDebugBuild false
//            signingConfig signingConfigs.release
        }
    }

      lintOptions {
          abortOnError false // FIXME!!!
      }

}

dependencies {
    implementation 'com.android.support:support-v4:26.+'
    implementation 'com.illposed.osc:javaosc-ui:0.2@jar'
}
